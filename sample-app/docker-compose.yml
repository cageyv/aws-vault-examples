version: '3.9'
services:
  sample-app:
    build:
      context: .
    restart: always
    environment:
      DEBUG: "*"
      AWS_REGION: "eu-central-1"
      AWS_ACCESS_KEY_ID:
      AWS_SECRET_ACCESS_KEY:
      AWS_SESSION_TOKEN:
    network_mode: host    
